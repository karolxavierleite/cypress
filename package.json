{
  "scripts": {
    "test": "cypress run",
    "test-ci": "cypress run --reporter mocha-multi-reporters --reporter-options configFile=cypress-reporter.json",
    "e2e:record": "cypress run --spec 'cypress/integration/form_authentication.js' --reporter mocha-multi-reporters --reporter-options configFile=cypress-reporter.json"
  },
  "private": true,
  "dependencies": {
    "cpf_cnpj": "^0.2.0",
    "cypress": "^2.1.0",
    "rosie": "^1.6.0"
  },
  "devDependencies": {
    "mocha": "^4.0.1",
    "mocha-junit-reporter": "^1.13.0",
    "mocha-multi-reporters": "^1.1.4"
  }
}
